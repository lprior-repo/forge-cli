import json

def lambda_handler(event, context):
    """
    Lambda function handler for {{ .Name }}
    """
    name = event.get('name', 'World')

    return {
        'statusCode': 200,
        'body': json.dumps({
            'message': f'Hello, {name}!',
            'success': True
        })
    }
